/**
 * @internal Only contains the props required by TypeScript.
 */
export interface Booking {
    /**
     * @format date-time
     */
    start: string;
    /**
     * @format date-time
     */
    end: string;
}